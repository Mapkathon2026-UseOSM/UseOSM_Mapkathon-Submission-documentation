# Glossary

| Term | Definition |
|---|---|
| LGA | Local Government Area — Nigeria's third-tier administrative division. |
| Isochrone | A polygon representing all points reachable within a given travel time from a location. |
| CRS / EPSG | Coordinate Reference System / its numeric code identifier; _TODO: which codes this project uses and why (UTM zone logic in `clean.py`)._ |
| Okada | Motorcycle taxi, a common transport mode in Nigeria; modeled as a distinct travel mode in the accessibility scoring. |
| Accessibility deficit score | _TODO: definition per `add_access_deficit_score()`._ |
| Completeness flag | _TODO: definition per `flag_completeness()`._ |
| Fishnet grid | _TODO: definition per `build_grid()`._ |
